#summary Deployment instructions.
#labels Featured,Phase-Deploy

=Installation=

==Maven 2==

The artifacts are deployed on `dev.java.net` repository. You have to add the following lines to enable it.
{{{
<repository>
  <id>maven2-repository.dev.java.net</id>
  <name>Java.net Repository for Maven</name>
  <url>http://download.java.net/maven/2/</url>
  <layout>default</layout>
</repository>
}}}

If you want to use the stream utilities add the following dependency to your pom.xml:

{{{
<dependency>
	<groupId>net.sf.jsignature.io-tools</groupId>
	<artifactId>easystream</artifactId>
	<version>[1.1.5,)</version>
	<type>jar</type>
</dependency>
}}}

While if you're interested in the format detection library you have to add:

{{{
<dependency>
	<groupId>net.sf.jsignature.io-tools</groupId>
	<artifactId>wazformat</artifactId>
	<version>[1.1.5,)</version>
	<type>jar</type>
</dependency>
}}}

==Other==
Non maven2 users should download the zip distribution and unpack it. All the jars in the `lib` directory of the distribution zip must be added to your classpath in order 
to get the libraries working.